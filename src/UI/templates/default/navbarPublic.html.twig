<nav class="navbar navbar-default">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">Travel Yeah</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li><a href="{{ path('homepage') }}">Home</a></li>
                <li><a href="{{ path('homepage') }}#favoritplaces">Most popular travels</a></li>
                {% if app.user %}
                    <li><a href="{{ path('main_private') }}">Private Area</a></li>
                    <li><a href="{{ path('main_private') }}">My travels</a></li>
                    <li><a href="{{ path('newTravel') }}">Add Travel</a></li>
                {% endif %}
                <li><a href="{{ path('homepage') }}#about">About</a></li>
                <li><a href="{{ path('homepage') }}#contact">Contact</a></li>
            </ul>
            <ul class="nav pull-right">
                <li class="divider-vertical"></li>

                {% if app.user %}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                            <span class="glyphicon glyphicon-user"></span> {{ app.user.username }} <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-right">
                            <li><a href="{{ path('profile_edit', {'_locale': 'en'}) }}"><span class="glyphicon glyphicon-cog"></span> My Profile</a></li>
                            <li role="separator" class="divider"></li>
                            <li><a href="{{ path('logout') }}"><span class="glyphicon glyphicon-log-out"></span> Logout</a></li>
                        </ul>
                    </li>
                {% elseif not app.user %}
                    <li><a href="{{ path('private_login') }}"><button type="button" class="btn btn-success navbar-btn">Sign in</button></a></li>
                {% endif %}
            </ul>


        </div>

    </div>
</nav>
