<nav class="navbar navbar-default">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">Travel Yeah</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li><a href="{{ path('homepage') }}">Home</a></li>
                <li><a href="{{ path('homepage') }}#favoritplaces">Most popular travels</a></li>
                {% if app.user %}
                    <li><a href="{{ path('main_private') }}">Private Area</a></li>
                    <li><a href="#">My travels</a></li>
                    <li><a href="#">Add Travel</a></li>
                {% endif %}
                <li><a href="{{ path('homepage') }}#about">About</a></li>
                <li><a href="{{ path('homepage') }}#contact">Contact</a></li>
            </ul>
            <ul class="nav pull-right">
                <li class="divider-vertical"></li>

                {% if app.user %}
                    <li><a href="{{ path('logout') }}">Logout</a></li>
                    <li>Logged as {{ app.user.username}}</li>
                {% elseif not app.user %}
                    <li><a href="{{ path('private_login') }}"><button type="button" class="btn btn-success navbar-btn">Sign in</button></a></li>
                {% endif %}
            </ul>


        </div>

    </div>
</nav>