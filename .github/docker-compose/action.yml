name: "Docker Compose"
description: "Install Docker Compose"
runs:
  using: "composite"
  steps:
    - name: install-dockercompose
      shell: bash
      env:
        DOCKER_COMPOSE_VERSION: '2.3.5'
      run: $GITHUB_ACTION_PATH/install-docker-compose.sh
